Allows logged users to chat with each other.
Users are authenticated with Firebase authentication and stored in Firebase storage.
Push notifications configured.
Developed in Flutter/Dart with Firebase.
